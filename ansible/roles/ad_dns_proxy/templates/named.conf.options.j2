options {
    directory "/var/cache/bind";

    forwarders {
        8.8.8.8;  # Google DNS (adjust as needed)
        8.8.4.4;
    };

    auth-nxdomain no;    # Conform to RFC1035
    listen-on-v6 { any; };
};
